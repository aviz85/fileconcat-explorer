{
  "name": "fileconcat-explorer",
  "version": "1.0.0",
  "description": "A web application for browsing and concatenating files",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "server": "nodemon server/server.js",
    "client": "npm start --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "install": "cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["file", "concatenation", "browser"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^6.2.0",
    "nodemon": "^2.0.7"
  }
}